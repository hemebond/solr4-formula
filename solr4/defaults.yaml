# -*- coding: utf-8 -*-
# vim: ft=yaml
solr4:
  version: 4.10.4
  archive:
    host: http://archive.apache.org
    path: /dist/lucene/solr
    format: tar
  install_dir: /opt
  data_dir: /var/solr
  config_dir: /etc/solr4
  user: solr
  group: solr
  service:
    name: solr
